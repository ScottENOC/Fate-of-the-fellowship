name: Apply Legacy boon wiring

on:
  push:
    branches: [feature/legacy-boon-wiring-v1]

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[boon-patch]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feature/legacy-boon-wiring-v1
      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Apply deterministic patch
        run: node scripts/apply-legacy-boon-wiring.cjs
      - name: Run regressions
        run: npm test
      - name: Commit patched source
        shell: bash
        run: |
          if git diff --quiet; then
            echo 'No source changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add engine.js tests/regression.cjs
          git commit -m 'Wire Legacy boons into gameplay [boon-patch]'
          git push origin HEAD:feature/legacy-boon-wiring-v1
