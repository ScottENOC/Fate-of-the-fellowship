'use strict';
const fs=require('node:fs');
function rep(s,a,b,label){if(!s.includes(a))throw new Error('missing '+label);return s.replace(a,b);}

let d=fs.readFileSync('data.js','utf8');
d=rep(d,
"  { key:'minas-morgul-yellow-extra', start:'minas-morgul', lineColor:'yellow', lineId:'yellow-c', destination:'minas-tirith', back:'red' },",
"  { key:'udun-yellow-extra', start:'udun', lineColor:'yellow', lineId:'yellow-c', destination:'minas-tirith', back:'red' },",
'Udun yellow extra route');
d=rep(d,
"text:'Add 1 shadow troop to Udûn, Barad-dûr, and Minas Morgul.'",
"text:'Add 1 Shadow troop to each Shadow stronghold in Mordor.'",
'Drums text');
d=rep(d,
"text:'Break Oath: remove 1 Dwarven troop from Iron Hills and 1 from Ered Luin.'",
"text:'Choose 1: remove any 2 friendly troops from the board; the current player discards a total of 2 cards and/or tokens; or lose 1 Hope.'",
'Wheels text');
fs.writeFileSync('data.js',d);

let e=fs.readFileSync('engine.js','utf8');
e=rep(e,
"      isShadowStronghold: false,",
"      isShadowStronghold: !!LOCS[id].capturable || ['udun','barad-dur','minas-morgul'].includes(id),",
'initial stronghold state');
e=rep(e,
"  s.ui = Object.assign({ selectedChar:null, pendingAction:null, validTargets:[], ignoreNextOrder:false, freeSearchThisTurn:false }, s.ui || {});",
"  s.ui = Object.assign({ selectedChar:null, pendingAction:null, validTargets:[], ignoreNextOrder:false, freeSearchThisTurn:false, pendingWheels:false }, s.ui || {});\n  if (!Number.isFinite(s.shadowDrawsRemaining)) s.shadowDrawsRemaining = 0;",
'migration wheels state');
e=rep(e,
"    ui: { selectedChar: null, pendingAction: null, validTargets: [], ignoreNextOrder: false, freeSearchThisTurn: false },",
"    ui: { selectedChar: null, pendingAction: null, validTargets: [], ignoreNextOrder: false, freeSearchThisTurn: false, pendingWheels:false },\n    shadowDrawsRemaining: 0,",
'new game wheels state');

const drawStart=e.indexOf('function drawShadowCards() {');
const drawEnd=e.indexOf('function resolveShadowCard(card) {',drawStart);
if(drawStart<0||drawEnd<0)throw new Error('shadow draw anchors missing');
const drawFns=`function drawShadowCards() {\n  if (G.phase !== 'draw-shadow' || G.ui.pendingWheels) return;\n  if (!G.shadowDrawsRemaining) {\n    G.shadowDrawsRemaining = G.threatRate;\n    if (G.shadowLieutenants.includes('mouth-of-sauron')) G.ui.pendingMouthTribute = true;\n  }\n  return continueShadowDraws();\n}\n\nfunction continueShadowDraws() {\n  const drawn=[];\n  while (G.shadowDrawsRemaining > 0 && !G.ui.pendingWheels && G.phase === 'draw-shadow') {\n    if (G.shadowDeck.length === 0) {\n      addLog('Shadow deck empty — reshuffling discard.');\n      G.shadowDeck = shuffle(G.shadowDiscard);\n      G.shadowDiscard = [];\n    }\n    if (G.shadowDeck.length === 0) { loseHope(1,'No shadow cards'); G.shadowDrawsRemaining--; continue; }\n    const card=G.shadowDeck.pop();\n    G.shadowDiscard.push(card);\n    G.shadowDrawsRemaining--;\n    drawn.push(card);\n    addLog('Shadow card: '+card.name);\n    resolveShadowCard(card);\n  }\n  if (G.shadowDrawsRemaining <= 0 && !G.ui.pendingWheels && G.phase === 'draw-shadow') {\n    G.shadowDrawsRemaining=0;\n    endTurn();\n  }\n  return {drawn,pendingWheels:!!G.ui.pendingWheels};\n}\n\n`;
e=e.slice(0,drawStart)+drawFns+e.slice(drawEnd);

const specStart=e.indexOf('function resolveSpecialShadow(card) {');
const specEnd=e.indexOf('// ── EVENT CARD EFFECTS',specStart);
if(specStart<0||specEnd<0)throw new Error('special anchors missing');
const special=`function resolveSpecialShadow(card) {\n  if (card.effect === 'drums') {\n    addLog('DRUMS OF WAR! Reinforce every current Shadow stronghold in Mordor.');\n    const targets=Object.entries(G.locState).filter(([locId,ls]) =>\n      LOCS[locId]?.region === 'mordor' && ls.isShadowStronghold\n    );\n    for (const [locId,ls] of targets) {\n      if (G.shadowSupply > 0) {\n        ls.shadowTroops++; G.shadowSupply--;\n        addLog('  +1 shadow troop at '+LOCS[locId].name+'.');\n        if (totalFriendlyAt(locId)>0) rollBattle(locId,ls.shadowTroops,null); else checkHavenLost(locId);\n      } else loseHope(1,'Shadow supply empty');\n    }\n    return;\n  }\n  if (card.effect === 'wheels') {\n    addLog('WHEELS OF SARUMAN! Current player must choose a consequence.');\n    G.ui.pendingWheels=true;\n  }\n}\n\nfunction wheelsFriendlyTroopCount(){\n  return Object.values(G.locState).reduce((sum,ls)=>sum+Object.values(ls.friendly||{}).reduce((a,b)=>a+b,0),0);\n}\nfunction wheelsDiscardableCount(){\n  const p=G.players[G.currentPlayer];\n  return (p.hand?.length||0)+Object.values(p.tokens||{}).reduce((a,b)=>a+b,0);\n}\nfunction resolveWheelsOfSaruman(choice,payload={}){\n  if(!G.ui.pendingWheels)return err('The Wheels of Saruman is not awaiting resolution.');\n  const p=G.players[G.currentPlayer];\n  if(choice==='hope'){\n    loseHope(1,'The Wheels of Saruman');\n  } else if(choice==='troops'){\n    if(wheelsFriendlyTroopCount()<2)return err('There are not 2 friendly troops on the board.');\n    const picks=Array.isArray(payload.picks)?payload.picks:[];\n    if(picks.length!==2)return err('Choose exactly 2 friendly troops.');\n    const need={};\n    for(const pick of picks){const locId=pick?.locId,type=pick?.type,key=locId+'|'+type;if(!G.locState[locId]?.friendly||!Object.hasOwn(G.locState[locId].friendly,type))return err('Invalid friendly troop choice.');need[key]=(need[key]||0)+1;}\n    for(const [key,n] of Object.entries(need)){const [locId,type]=key.split('|');if((G.locState[locId].friendly[type]||0)<n)return err('Not enough selected troops at that location.');}\n    for(const pick of picks){G.locState[pick.locId].friendly[pick.type]--;G.troopSupply[pick.type]=(G.troopSupply[pick.type]||0)+1;}\n    addLog('Wheels of Saruman: 2 friendly troops removed from the board.');\n  } else if(choice==='resources'){\n    if(wheelsDiscardableCount()<2)return err('The current player does not have 2 cards/tokens to discard.');\n    const cardIds=Array.isArray(payload.cardIds)?payload.cardIds:[];\n    const tokenCounts=payload.tokenCounts||{};\n    const total=cardIds.length+Object.values(tokenCounts).reduce((a,b)=>a+(Number(b)||0),0);\n    if(total!==2)return err('Discard exactly 2 cards/tokens in total.');\n    if(new Set(cardIds).size!==cardIds.length)return err('A card can only be discarded once.');\n    for(const id of cardIds)if(!p.hand.some(c=>c.id===id))return err('Selected card is not in hand.');\n    for(const [sym,n0] of Object.entries(tokenCounts)){const n=Number(n0)||0;if(n<0||(p.tokens[sym]||0)<n)return err('Not enough selected tokens.');}\n    for(const id of cardIds){const idx=p.hand.findIndex(c=>c.id===id);G.playerDiscard.push(p.hand.splice(idx,1)[0]);}\n    for(const [sym,n0] of Object.entries(tokenCounts))p.tokens[sym]-=(Number(n0)||0);\n    addLog('Wheels of Saruman: current player discarded 2 cards/tokens.');\n  } else return err('Choose a valid Wheels of Saruman consequence.');\n  G.ui.pendingWheels=false;\n  if(G.phase!=='gameover')continueShadowDraws();\n  return {ok:true};\n}\n\n`;
e=e.slice(0,specStart)+special+e.slice(specEnd);
e=rep(e,
"  G.ui = { selectedChar: null, pendingAction: null, validTargets: [], ignoreNextOrder: G.ui.ignoreNextOrder, freeSearchThisTurn: false };",
"  G.ui = { selectedChar: null, pendingAction: null, validTargets: [], ignoreNextOrder: G.ui.ignoreNextOrder, freeSearchThisTurn: false, pendingWheels:false };",
'end turn ui');
fs.writeFileSync('engine.js',e);

let h=fs.readFileSync('index.html','utf8');
h=rep(h,
"  // Mouth of Sauron tribute prompt\n  if (G?.ui?.pendingMouthTribute) {",
"  if (G?.ui?.pendingWheels && !window.__wheelsPromptOpen) {\n    window.__wheelsPromptOpen=true;\n    setTimeout(showWheelsChoice,100);\n  }\n  // Mouth of Sauron tribute prompt\n  if (G?.ui?.pendingMouthTribute) {",
'render wheels prompt');
const anchor='function showUtilityMenu() {';
if(!h.includes(anchor))throw new Error('utility anchor missing');
const ui=`let _wheelsTroopPicks=[];\nlet _wheelsResourcePicks={cardIds:[],tokenCounts:{}};\nfunction finishWheels(choice,payload={}){const r=resolveWheelsOfSaruman(choice,payload);if(r?.error){showToast(r.error);return;}window.__wheelsPromptOpen=false;closeModal();render();}\nfunction showWheelsChoice(){\n  if(!G?.ui?.pendingWheels){window.__wheelsPromptOpen=false;return;}\n  const buttons=[];\n  if(wheelsFriendlyTroopCount()>=2)buttons.push({label:'Remove 2 friendly troops',action:()=>{_wheelsTroopPicks=[];showWheelsTroopPicker();}});\n  if(wheelsDiscardableCount()>=2)buttons.push({label:'Discard 2 cards/tokens',action:()=>{_wheelsResourcePicks={cardIds:[],tokenCounts:{}};showWheelsResourcePicker();}});\n  buttons.push({label:'Lose 1 Hope',action:()=>finishWheels('hope')});\n  showModal('⚙ The Wheels of Saruman','Choose one consequence. Options you cannot fully pay are not shown.',buttons);\n}\nfunction showWheelsTroopPicker(){\n  const groups=[];for(const [locId,ls] of Object.entries(G.locState))for(const [type,n] of Object.entries(ls.friendly||{}))if(n>0)groups.push({locId,type,n});\n  const used={};for(const p of _wheelsTroopPicks){const k=p.locId+'|'+p.type;used[k]=(used[k]||0)+1;}\n  const buttons=[];for(const g of groups){const k=g.locId+'|'+g.type,u=used[k]||0;if(u<g.n&&_wheelsTroopPicks.length<2)buttons.push({label:'+ '+LOCS[g.locId].name+' — '+g.type+' ('+(g.n-u)+' left)',action:()=>{_wheelsTroopPicks.push({locId:g.locId,type:g.type});showWheelsTroopPicker();}});}\n  if(_wheelsTroopPicks.length)buttons.push({label:'Undo last',action:()=>{_wheelsTroopPicks.pop();showWheelsTroopPicker();}});\n  if(_wheelsTroopPicks.length===2)buttons.push({label:'Confirm 2 troops',action:()=>finishWheels('troops',{picks:_wheelsTroopPicks})});\n  buttons.push({label:'← Back',action:showWheelsChoice});\n  const names=_wheelsTroopPicks.map(p=>LOCS[p.locId].name+' — '+p.type).join('<br>')||'None yet';\n  showModal('Wheels — remove troops','Choose exactly 2 friendly troops.<br><br><strong>Selected:</strong><br>'+names,buttons);\n}\nfunction showWheelsResourcePicker(){\n  const p=G.players[G.currentPlayer],picked=new Set(_wheelsResourcePicks.cardIds);\n  const total=_wheelsResourcePicks.cardIds.length+Object.values(_wheelsResourcePicks.tokenCounts).reduce((a,b)=>a+b,0);\n  const buttons=[];\n  if(total<2){for(const c of p.hand)if(!picked.has(c.id))buttons.push({label:'+ Card: '+c.name,action:()=>{_wheelsResourcePicks.cardIds.push(c.id);showWheelsResourcePicker();}});for(const [sym,n] of Object.entries(p.tokens)){const u=_wheelsResourcePicks.tokenCounts[sym]||0;if(u<n)buttons.push({label:'+ Token: '+(SYM[sym]?.icon||sym)+' '+(SYM[sym]?.name||sym),action:()=>{_wheelsResourcePicks.tokenCounts[sym]=u+1;showWheelsResourcePicker();}});}}\n  if(total>0)buttons.push({label:'Reset selection',action:()=>{_wheelsResourcePicks={cardIds:[],tokenCounts:{}};showWheelsResourcePicker();}});\n  if(total===2)buttons.push({label:'Confirm 2 resources',action:()=>finishWheels('resources',_wheelsResourcePicks)});\n  buttons.push({label:'← Back',action:showWheelsChoice});\n  const cards=_wheelsResourcePicks.cardIds.map(id=>p.hand.find(c=>c.id===id)?.name||id);const toks=Object.entries(_wheelsResourcePicks.tokenCounts).flatMap(([s,n])=>Array(n).fill((SYM[s]?.icon||s)+' '+(SYM[s]?.name||s)));\n  showModal('Wheels — discard resources','Choose exactly 2 cards/tokens in any combination.<br><br><strong>Selected:</strong><br>'+([...cards,...toks].join('<br>')||'None yet'),buttons);\n}\n\n`;
h=h.replace(anchor,ui+anchor);
fs.writeFileSync('index.html',h);

let t=fs.readFileSync('tests/regression.cjs','utf8');
const oldDrums="test('Drums of War reinforces Udun, Barad-dur and Minas Morgul', () => {";
if(t.includes(oldDrums)){
  const start=t.indexOf(oldDrums),next=t.indexOf("\ntest('",start+10);if(next>start)t=t.slice(0,start)+t.slice(next+1);
}
const testAnchor="test('Shadow Burdens are tier-gated, unique and modify setup state', () => {";
if(!t.includes(testAnchor))throw new Error('test anchor missing');
const add=`test('clarified Shadow extras include Udun yellow and omit Udun green', () => {\n  const ctx=makeContext();\n  assert.equal(evalIn(ctx,"NORMAL_SHADOW_CARDS.some(c=>c.location==='udun'&&c.lineColor==='yellow')"),true);\n  assert.equal(evalIn(ctx,"NORMAL_SHADOW_CARDS.some(c=>c.location==='udun'&&c.lineColor==='green')"),false);\n});\n\ntest('Drums of War dynamically reinforces current Mordor Shadow strongholds', () => {\n  const ctx=makeContext();startGame(ctx,{numPlayers:1,playerNames:['Tester'],charAssignment:[['frodo-sam','aragorn']],difficulty:'standard',cardPrefs:{},boons:{}});\n  vm.runInContext("G.locState.udun.shadowTroops=0;G.locState['barad-dur'].shadowTroops=0;G.locState['minas-morgul'].shadowTroops=0;G.locState['barad-dur'].isShadowStronghold=false;G.shadowSupply=10;resolveSpecialShadow(SPECIAL_SHADOW_CARDS.find(c=>c.effect==='drums'));",ctx);\n  assert.equal(evalIn(ctx,'G.locState.udun.shadowTroops'),1);\n  assert.equal(evalIn(ctx,"G.locState['minas-morgul'].shadowTroops"),1);\n  assert.equal(evalIn(ctx,"G.locState['barad-dur'].shadowTroops"),0);\n});\n\ntest('Wheels of Saruman allows legal hope, troop, and mixed resource choices', () => {\n  const ctx=makeContext();startGame(ctx,{numPlayers:1,playerNames:['Tester'],charAssignment:[['frodo-sam','aragorn']],difficulty:'standard',cardPrefs:{},boons:{}});\n  vm.runInContext("G.ui.pendingWheels=true;__h=G.hope;__r=resolveWheelsOfSaruman('hope')",ctx);assert.equal(evalIn(ctx,'__r.ok'),true);assert.equal(evalIn(ctx,'G.hope'),evalIn(ctx,'__h')-1);\n  vm.runInContext("G.phase='actions';G.ui.pendingWheels=true;G.locState['minas-tirith'].friendly.gondor=2;__r2=resolveWheelsOfSaruman('troops',{picks:[{locId:'minas-tirith',type:'gondor'},{locId:'minas-tirith',type:'gondor'}]})",ctx);assert.equal(evalIn(ctx,'__r2.ok'),true);assert.equal(evalIn(ctx,"G.locState['minas-tirith'].friendly.gondor"),0);\n  vm.runInContext("G.ui.pendingWheels=true;G.players[0].tokens.valor=1;G.players[0].hand=[{id:'x',name:'X'}];G.playerDiscard=[];__r3=resolveWheelsOfSaruman('resources',{cardIds:['x'],tokenCounts:{valor:1}})",ctx);assert.equal(evalIn(ctx,'__r3.ok'),true);assert.equal(evalIn(ctx,'G.players[0].hand.length'),0);assert.equal(evalIn(ctx,'G.players[0].tokens.valor'),0);\n});\n\ntest('Wheels hides impossible consequences in the UI', () => {\n  assert.ok(indexSource.includes("if(wheelsFriendlyTroopCount()>=2)"));\n  assert.ok(indexSource.includes("if(wheelsDiscardableCount()>=2)"));\n  assert.ok(indexSource.includes("buttons.push({label:'Lose 1 Hope'"));\n});\n\n`;
t=t.replace(testAnchor,add+testAnchor);
fs.writeFileSync('tests/regression.cjs',t);
