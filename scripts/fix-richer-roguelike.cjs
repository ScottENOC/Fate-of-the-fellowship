'use strict';
const fs = require('node:fs');
const path = require('node:path');
const p = path.join(__dirname, 'apply-richer-roguelike.cjs');
let s = fs.readFileSync(p, 'utf8');
s = s.replace('move 1 Nazgûl from Mordor to Enedwaith.', 'move 1 Nazgûl from Mordor to Rhudaur.');
s = s.replace("to:'enedwaith'", "to:'rhudaur'");
s = s.replace("assert.equal(g.nazgul.enedwaith, 1);", "assert.equal(g.nazgul.rhudaur, 2);");
fs.writeFileSync(p, s);
console.log('Richer roguelike Nazgul target repaired.');
